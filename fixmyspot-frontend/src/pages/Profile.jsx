import { useAuth } from "../context/AuthContext";
import { useReports } from "../context/ReportsContext";
export default function Profile(){const {user}=useAuth();const {reports}=useReports();const mine=reports.filter(r=>r.reporter?._id===user.id||r.reporter===user.id);return <><section className="page-heading"><div><span className="eyebrow">ACCOUNT</span><h1>My profile</h1><p>Your account data comes from MongoDB.</p></div></section><div className="profile-grid"><section className="profile-card"><div className="profile-avatar">{user.name[0].toUpperCase()}</div><h2>{user.name}</h2><p>{user.email}</p><p>Role: <strong>{user.role}</strong></p><p>Area: {user.area}</p></section><section className="profile-card"><h2>Your contribution</h2><div className="big-number">{mine.length}</div><span>Reports submitted</span></section></div></>;}
